name: admet-chemprop
channels:
  - defaults
dependencies:
  - python=3.11.15
  - pip=26.1.2
  - pip:
      - --extra-index-url https://download.pytorch.org/whl/cpu
      - torch==2.6.0+cpu
      - -r requirements-chemprop.txt
